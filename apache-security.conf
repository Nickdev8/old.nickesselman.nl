# The application source is mounted into Apache's document root. Never expose
# repository metadata, which can include remotes and deployment information.
<Directory "/var/www/html/.git">
    Require all denied
</Directory>
